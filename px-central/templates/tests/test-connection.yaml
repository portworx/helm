apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "px-central.fullname" . }}-test-connection"
  labels:
    {{- include "px-central.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "px-central.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

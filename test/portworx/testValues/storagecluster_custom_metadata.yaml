customMetadata:
  annotations:
    pod:
      storage: "application=my-app;environment=testing"
    service:
      portworx-api: "application=my-app;environment=testing"
      portworx-service: "application=my-app;environment=testing"
      portworx-kvdb-service: "application=my-app;environment=testing"
  labels:                          
    service: 
      portworx-api: "application=my-app;environment=testing"
    deployment:
      "*": "application=my-app;environment=testing"
      autopilot: "application=my-app;environment=testing"
      px-csi-ext: "application=my-app;environment=testing"
      px-plugin: "application=my-app;environment=testing"
      px-plugin-proxy: "application=my-app;environment=testing"
      portworx-pvc-controller: "application=my-app;environment=testing"
      stork: "application=my-app;environment=testing"
      stork-scheduler: "application=my-app;environment=testing"
      px-prometheus-operator: "application=my-app;environment=testing"
    daemonset:
      "*": "application=my-app;environment=testing"
      portworx-api: "application=my-app;environment=testing"
      portworx-proxy: "application=my-app;environment=testing"
      px-telemetry-phonehome: "application=my-app;environment=testing"
    prometheus:
      px-prometheus: "application=my-app;environment=testing"
    alertmanager:
      portworx: "application=my-app;environment=testing"
    pod:
      "*": "application=my-app;environment=testing"
      storage: "application=my-app;environment=testing"
      portworx-kvdb: "application=my-app;environment=testing"

Your Release is named {{ .Release.Name | quote }}

NOTE:
Wait for px-central setup to complete, pxcentral-post-install-hook to complete.
watch kubectl get po --namespace {{ .Release.Namespace }} -ljob-name=pxcentral-post-install-hook  -o wide | awk '{print $1, $3}' | grep -iv error

PX-Central-Onprem can be accessed:

PX-Central Access URL:  http://<INGRESS_ENDPOINT>/pxcentral
PX-Central OIDC Access URL:  http://<INGRESS_ENDPOINT>/keycloak

Login with the following default credentials:
Username: {{ .Values.centralOIDC.username }}
Password: {{ .Values.centralOIDC.password }}

To retrieve the onprem central install status information, check the pxcentral-post-install-hook pods logs in namespace: {{ .Release.Namespace }}.
To get the INGRESS_ENDPOINT, check ingress *pxcentral-onprem-central-ingress* endpoint in namespace: {{ .Release.Namespace }}.

Commands to get INGRESS_ENDPOINT:
IP: kubectl get ingress --namespace {{ .Release.Namespace }} pxcentral-onprem-central-ingress -o jsonpath="{.status.loadBalancer.ingress[0].ip}"
Hostname: kubectl get ingress --namespace {{ .Release.Namespace }} pxcentral-onprem-central-ingress -o jsonpath="{.status.loadBalancer.ingress[0].hostname}"

To retrive pre-install hooks logs, use command:
kubectl logs --namespace {{ .Release.Namespace }} $(kubectl get po --namespace {{ .Release.Namespace }} -ljob-name=pxcentral-pre-install-hook | awk '{print $1}' | grep -iv error | grep -v NAME)

To retrive post-install hooks logs, use command:
kubectl logs --namespace {{ .Release.Namespace }} $(kubectl get po --namespace {{ .Release.Namespace }} -ljob-name=pxcentral-post-install-hook | awk '{print $1}' | grep -iv error | grep -v NAME)